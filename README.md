# EM forces interpolator

first load of the GUI may take some time, subsequent runs are much faster

## Quickstart

First, after downloading the package, install it using:

```
pip install .
```

After that, start the GUI using:

```
python XXX
```